{
  "name": "elecc19nes-gemini-functions",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=lib/index.js --format=cjs --external:firebase-admin --external:firebase-functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "~5.8.3"
  }
}
